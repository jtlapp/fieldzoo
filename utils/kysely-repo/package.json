{
  "name": "@fieldzoo/kysely-repo",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\"",
    "test": "jest --passWithNoTests"
  },
  "jest": {
    "preset": "../../config/jest-preset.node.js"
  },
  "prettier": "../../config/prettierrc.json",
  "dependencies": {
    "kysely": "^0.23.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.3",
    "better-sqlite3": "^8.1.0"
  }
}
