{
  "name": "@fieldzoo/freeze-field",
  "version": "0.0.0",
  "private": true,
  "main": "dist/freeze-field.js",
  "source": "src/freeze-field.ts",
  "types": "src/freeze-field.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\"",
    "test": "jest"
  },
  "jest": {
    "preset": "jest-presets/node"
  },
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "eslint-config-custom": "workspace:*",
    "jest": "^29.4.3",
    "jest-presets": "workspace:*",
    "ts-jest": "^29.0.5",
    "tsconfig": "workspace:*",
    "typescript": "^4.9.5"
  }
}
