{
  "name": "@fieldzoo/testing-utils",
  "version": "0.0.0",
  "private": true,
  "main": "dist/testing-utils.js",
  "source": "src/testing-utils.ts",
  "types": "src/testing-utils.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\""
  },
  "jest": {
    "preset": "jest-presets/node"
  },
  "prettier": "../../config/prettierrc.json",
  "dependencies": {},
  "devDependencies": {
    "jest-presets": "workspace:*"
  }
}
