{
  "name": "@fieldzoo/utilities",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\""
  },
  "jest": {
    "preset": "jest-presets/node"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "jest-presets": "workspace:*",
    "tsconfig": "workspace:*"
  },
  "dependencies": {
    "kysely": "^0.23.4"
  }
}
