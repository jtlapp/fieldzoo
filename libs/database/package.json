{
  "name": "@fieldzoo/database",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\""
  },
  "jest": {
    "preset": "jest-presets/node"
  },
  "dependencies": {
    "@fieldzoo/config": "workspace:*",
    "@fieldzoo/utilities": "workspace:*",
    "dotenv": "^16.0.3",
    "kysely": "^0.23.4",
    "pg": "^8.9.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.17",
    "@types/pg": "^8.6.6",
    "eslint-config-custom": "workspace:*",
    "jest-presets": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
