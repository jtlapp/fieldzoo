{
  "name": "@fieldzoo/util-lib-template",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "clean": "rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "TIMING=1 eslint \"src/**/*.ts*\"",
    "test": "jest --passWithNoTests"
  },
  "jest": {
    "preset": "jest-presets/node"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "eslint": "^8.40.0",
    "eslint-config-custom": "workspace:*",
    "jest": "^29.5.0",
    "jest-presets": "workspace:*",
    "ts-jest": "^29.1.0",
    "tsconfig": "workspace:*",
    "typescript": "5.1.3"
  }
}
